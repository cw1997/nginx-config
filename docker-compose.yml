version: '3'
services:
  web:
    container_name: nginx-server
    image: changwei1997/nginx:latest
    build: .
    ports:
      - "80:80"
    volumes:
      - "/var/docker/nginx/www:/usr/share/nginx/html"
      - "/var/docker/nginx/conf:/etc/nginx"
      - "/var/docker/nginx/logs:/var/log/nginx"
    expose:
      - "80"
# volumes:
#   nginx: {}
  